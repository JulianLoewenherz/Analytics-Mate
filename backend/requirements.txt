# Video analytics backend dependencies
fastapi==0.104.1          # Web framework for API endpoints
uvicorn[standard]==0.24.0 # ASGI server to run FastAPI
python-multipart==0.0.6   # Required for file upload handling
opencv-python==4.10.0.84  # Video processing and metadata extraction
numpy==1.26.4             # Required by OpenCV (Python 3.13 compatible)

# Phase 1: Vision + Pipeline
ultralytics>=8.3.0        # YOLO detection, tracking (ByteTrack), and pose
shapely>=2.0.0            # Polygon geometry for ROI point-in-polygon

# Phase 2: LLM Planner
openai>=1.0.0             # OpenAI API for GPT-4o-mini (prompt -> plan)
